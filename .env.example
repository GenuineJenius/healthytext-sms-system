# HealthyText SMS System - Environment Configuration
# =====================================================
# Copy this file to .env and fill in your actual values

# Application Environment
NODE_ENV=development
PORT=3000

# Twilio Configuration (Required)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890
TWILIO_MESSAGING_SERVICE_SID=your_messaging_service_sid
ADMIN_PHONE_NUMBER=+1234567890

# Production Database (MariaDB) - ChemiCloud
# ===========================================
# If these are provided, the app will use MariaDB
# If not provided, will fallback to SQLite for local development

MARIADB_HOST=your_mariadb_host
MARIADB_USER=your_mariadb_username
MARIADB_PASSWORD=your_mariadb_password
MARIADB_DATABASE=healthytext_sms
MARIADB_PORT=3306

# WordPress Database (Read-Only) - For user sync
# ==============================================
WORDPRESS_DB_HOST=15.204.105.183
WORDPRESS_DB_PORT=3306
WORDPRESS_DB_USER=healthtxt_
WORDPRESS_DB_PASSWORD=pA0-w5Sf[7
WORDPRESS_DB_NAME=healthtxt_
WORDPRESS_DB_PREFIX=Healthtxttbl_
WORDPRESS_DB_CHARSET=utf8mb4

# Alternative names for WordPress (if needed)
WP_DB_HOST=15.204.105.183
WP_DB_USER=healthtxt_
WP_DB_PASSWORD=pA0-w5Sf[7
WP_DB_NAME=healthtxt_
WP_DB_PREFIX=Healthtxttbl_

# Application Settings
# ===================
DEFAULT_TIMEZONE=America/Chicago
MAX_MESSAGES_PER_DAY=4
SEND_TIME_WINDOW_START=09:00
SEND_TIME_WINDOW_END=18:00

# Logging & Monitoring
# ===================
LOG_LEVEL=info
ENABLE_DETAILED_LOGGING=true

# Security
# ========
# Add any API keys or security tokens here

# Development Settings (Local only)
# =================================
# These are ignored in production

# Local SQLite database paths (used when MariaDB not available)
SQLITE_MESSAGES_DB=./databases/messages.db
SQLITE_TRACKING_DB=./databases/user_tracking.db
SQLITE_LOGS_DB=./databases/system_logs.db

# Environment Detection Notes:
# ===========================
# - If MARIADB_HOST is set → Uses MariaDB (production)
# - If NODE_ENV=production → Uses MariaDB
# - Otherwise → Uses SQLite (development)
#
# WordPress Integration:
# - Always uses remote WordPress database for user data
# - Local SMS database stores user tracking and message history
#
# ChemiCloud Deployment:
# - Set NODE_ENV=production
# - Configure MariaDB credentials from cPanel
# - Ensure all TWILIO_* variables are set
# - WordPress credentials should work as-is